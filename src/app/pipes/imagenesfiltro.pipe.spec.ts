import { ImagenesfiltroPipe } from './imagenesfiltro.pipe';

describe('ImagenesfiltroPipe', () => {
  it('create an instance', () => {
    const pipe = new ImagenesfiltroPipe();
    expect(pipe).toBeTruthy();
  });
});
